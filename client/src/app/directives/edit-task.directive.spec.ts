import { EditTaskDirective } from './edit-task.directive';

describe('EditTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new EditTaskDirective();
    expect(directive).toBeTruthy();
  });
});
